version: "3.8"

services:
  writer-web:
    build:
      context: ..
      dockerfile: web/Dockerfile
    env_file: .env
    ports:
      - "18900:8000"
    volumes:
      - ./data:/app/web/data
    restart: unless-stopped
